export default function MainLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <>
            {/* <MainHeader /> */}
            {children}
        </>
    );
}

/// products/1234
